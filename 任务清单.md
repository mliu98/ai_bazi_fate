# 八字缘分测算 · Coding Agent 开发任务清单

> **技术栈**：React + Vite + TypeScript + Tailwind / shadcn-ui · Supabase Functions · DeepSeek API · Vercel + Supabase/Railway
> **执行方式**：按顺序完成，每个任务完成后标记 `[x]`，有依赖关系的任务请勿跳过前置任务

---

## 🗂 PHASE 1 · 项目初始化与基础架构


### 1.1 项目脚手架（基于当前仓库）

- [ ] 确认本地开发环境：Node.js（建议 18+）、pnpm/npm/yarn
- [ ] 前端已在仓库：使用 `src/main.tsx` 与 `src/app/` 目录作为入口，确认 `package.json` 脚本（`dev` / `build` / `preview`）
- [ ] 后端采用 Supabase Functions：查看 `supabase/functions/server/index.tsx`，确认运行方式与环境变量（`SUPABASE_URL`、`SUPABASE_SERVICE_KEY`、`DEEPSEEK_API_KEY`、`ADMIN_PASSWORD`）
- [ ] 配置 `.env.local`（前端 dev）与 `.env`（Supabase/后端）模板并加入 `.gitignore`
- [ ] 初始化 Git 分支策略：`main`（生产）+ `dev`（开发），并在 repo 内约定 Pull Request 流程

### 1.2 Supabase 数据库初始化

- [ ] 在 Supabase 创建项目，获取 URL 和 anon key
- [ ] 执行以下建表 SQL：

```sql
-- 激活码表
CREATE TABLE activation_codes (
  code        VARCHAR(9) PRIMARY KEY,
  type        TEXT CHECK (type IN ('free', 'full', 'unlimited')),
  total_uses  INT NOT NULL,
  used_count  INT DEFAULT 0,
  created_at  TIMESTAMPTZ DEFAULT NOW(),
  expires_at  TIMESTAMPTZ,
  note        TEXT
);

-- 测算记录表（可选，用于运营数据分析）
CREATE TABLE calculations (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  code        VARCHAR(9) REFERENCES activation_codes(code),
  created_at  TIMESTAMPTZ DEFAULT NOW(),
  score       INT,
  ip          TEXT
);
```

- [ ] 为 `activation_codes` 表的 `code` 字段设置 RLS（Row Level Security），仅允许后端 service key 写入
- [ ] 预生成 10 个体验码（type=free, total_uses=1）用于开发测试

### 1.3 项目目录结构规范

```
项目结构（仓库内现有路径）

src/
├── main.tsx                # React 入口
├── app/
│   ├── App.tsx
│   ├── routes.ts
│   ├── components/          # 复用组件（ui/、figma/ 等）
│   │   ├── figma/
│   │   └── ui/
│   └── pages/               # Activate.tsx, Input.tsx, Result.tsx 等
├── styles/
└── utils/

supabase/
├── functions/
│   └── server/              # 后端函数与 API（index.tsx, kv_store.tsx）
└── sql/

backend/ (可选)              # 如果需要独立服务，可放置 express/worker
```

---

## 🔐 PHASE 2 · 激活码系统


### 2.1 后端：激活码服务（Supabase Functions 实现）

- [ ] 在 `supabase/functions/server` 中实现 `codeService`：`generateCode()`、`verifyCode()`、`consumeCode()`、`batchGenerate()`，使用 Supabase 服务 key 写入/查询
- [ ] 激活码格式：`XXXX-XXXX`（排除易混字符），并在 `sql/init.sql` 中保证表结构一致
- [ ] 对并发消耗使用事务或乐观锁（`used_count` 并发更新校验）

### 2.2 后端：激活码接口

- [ ] `POST /api/code/verify`：接收 `{ code }`，调用 `verifyCode`，返回验证结果（不消耗次数）
- [ ] 为所有 `/api/calculate` 和 `/api/report/full` 接口添加 `auth.js` 中间件：请求头携带 `X-Activation-Code`，中间件校验并将 code 信息注入 `req.codeInfo`

### 2.3 前端：激活码输入页（`/activate`）

- [ ] 页面布局：产品 Logo + 一句话说明 + 激活码输入框（自动格式化为 `XXXX-XXXX`）+ 确认按钮
- [ ] 输入框实现：监听输入自动在第 4 位后插入 `-`，大写转换，长度限制 9 位
- [ ] 调用 `POST /api/code/verify`，校验通过后将 code 和 type 存入 Pinia session store 和 localStorage
- [ ] 错误状态处理：无效码 / 已过期 / 次数用完，分别展示不同提示文案
- [ ] 校验成功后路由跳转到 `/input`
- [ ] 全局路由守卫：无有效 code 在 session 中时，访问 `/input`、`/result` 等页面自动跳转到 `/activate`

---

## 📝 PHASE 3 · 用户输入流程

### 3.1 前端：信息输入页（`/input`）

- [ ] Step 1 — 生日输入：
  - 使用 React 组件（自定义 DatePicker 或现成轻量库）实现年/月/日选择，范围可配置
  - 时辰选择：12 个选项 + 「不知道」，展示为「子时（23-1点）」格式
  - 性别选择：大按钮单选，样式基于 Tailwind / shadcn 组件
  - 对方生日：同结构，字段名前缀 `partner_`
- [ ] Step 2 — 玄学问卷：
  - 5 道单选题，每题选项用大卡片按钮展示
  - 选中状态有明显高亮（红色描边 + 背景色）
  - 每次只显示一题，支持上一题/下一题导航，最后一题显示「开始测算」按钮
- [ ] 表单数据收集完毕后存入 Pinia store，跳转到 `/loading`

### 3.2 前端：Loading 页（`/loading`）

- [ ] 展示八字排盘动效（CSS 动画，天干地支字符轮播）
- [ ] 随机展示 5 条玄学语录，每 1.5 秒切换一条
- [ ] 同时触发 `POST /api/calculate` 请求
- [ ] 请求完成后等待至少 3 秒（体验感），然后跳转到 `/result`
- [ ] 请求失败处理：展示错误提示 + 重试按钮

---

## ⚙️ PHASE 4 · 八字计算引擎（后端）

> 注意：八字计算是纯规则逻辑，不依赖大模型，先完成此模块再接入 AI

### 4.1 八字基础计算

- [ ] `baziEngine.js`：实现 `getBazi(year, month, day, hour)` 函数
  - 计算年柱（天干地支）：以立春为界换年
  - 计算月柱：按节气（可使用查表法，内置 2000-2040 年节气表）
  - 计算日柱：使用公式法（蔡勒公式变体）计算日干支
  - 计算时柱：按时辰对应地支，天干由日干推算
  - 返回：`{ year: {gan, zhi}, month: {gan, zhi}, day: {gan, zhi}, hour: {gan, zhi} }`
- [ ] `baziEngine.js`：实现 `getWuxing(bazi)` 函数，统计四柱中金木水火土各元素数量
- [ ] 编写单元测试：至少 10 个已知生日的八字对照（可从网上查证），确保计算准确

### 4.2 合婚评分引擎

- [ ] `baziEngine.js`：实现合婚关系查表：
  - 六合表（子丑合、寅亥合、卯戌合、辰酉合、巳申合、午未合）
  - 三合表（申子辰、亥卯未、寅午戌、巳酉丑）
  - 六冲表（子午冲、丑未冲、寅申冲、卯酉冲、辰戌冲、巳亥冲）
  - 相克表（基于天干：甲克戊、乙克己等）
- [ ] `baziEngine.js`：实现 `calcCompatibility(bazi1, bazi2)` 函数：
  - 日柱相合 +20 分，月柱相合 +15 分，年柱相合 +10 分，时柱相合 +5 分
  - 日柱相冲 -15 分，月柱相冲 -10 分
  - 五行互补（双方五行相生）+10 分
  - 五行相克 -10 分
  - 基础分 50，最终分数限制在 55-99 范围内
  - 返回：`{ score, level, highlights_flags, warning_flags, wuxing_relation }`

---

## 🤖 PHASE 5 · AI 报告生成（后端）


### 5.1 DeepSeek API 集成（保留并优先）

- [ ] `aiService.ts`（或 `supabase/functions/server/aiService.tsx`）：封装 DeepSeek API 调用，base URL 为 `https://api.deepseek.com`，模型 `deepseek-chat` 或 `deepseek-v3`
- [ ] 请求需实现超时（15s）和重试（失败最多重试 2 次）策略，避免前端长时间卡死
- [ ] 响应解析：优先解析 JSON 输出，处理模型可能携带的额外文本或 markdown 包裹
- [ ] 在 free / full 两种激活码类型上控制输出字段：free 不返回 `warning` 与 `timing`，full 返回完整内容

### 5.2 Prompt 工程

- [ ] `aiService.js`：实现 `generateReport(baziData, surveyAnswers, codeType)` 函数
- [ ] Prompt 模板（系统提示）：

```
你是一位精通八字合婚的命理师，擅长用现代年轻人听得懂的语言解读命盘。
语言要求：口语化、有温度、带情绪感，初中语文水平即可读懂，适当使用 emoji。
禁止出现：「毫无缘分」「注定分离」「必然失败」等极端负面表述。
必须以 JSON 格式输出，不要输出任何其他内容。
```

- [ ] Prompt 用户输入结构：

```
八字信息：{bazi1} vs {bazi2}
五行关系：{wuxing_relation}
缘分得分：{score}（满分99）
等级：{level}
亮点标签：{highlight_flags}
隐患标签：{warning_flags}
问卷答案：{survey_answers}

请输出 JSON：
{
  "highlights": ["亮点1", "亮点2", "亮点3"],  // 3-5条，正向，每条30字内
  "advice": ["建议1", "建议2", "建议3"],        // 3条相处建议，口语化
  "sign": "专属姻缘签全文",                      // 60-80字，诗意，带情绪
  "share_text": "分享文案",                       // 80字内，适合小红书，带悬念
  "warning": "隐患预警内容",                      // 50字内，委婉表达
  "timing": "最佳发展时机描述"                    // 具体到月份，有温度
}
```

- [ ] 对 AI 输出做内容过滤：检查 `warning` 字段是否包含极端负面词汇，若有则替换为温和版本
- [ ] 如果 codeType 为 `free`，不生成 `warning` 和 `timing` 字段（节省 token）

### 5.3 报告生成接口

- [ ] `POST /api/calculate`：
  - 从请求体获取：`{ selfBirthday, partnerBirthday, selfGender, survey }`
  - 调用 `getBazi` 计算双方八字
  - 调用 `calcCompatibility` 获取评分和标签
  - 调用 `aiService.generateReport` 获取 AI 文案
  - 消耗激活码一次（调用 `consumeCode`）
  - 返回完整报告数据（免费版不含 warning/timing）
  - 记录一条 calculations 记录
- [ ] `POST /api/report/full`：
  - 校验 codeType 必须为 `full` 或 `unlimited`，否则返回 403
  - 重新生成含 warning + timing 的完整报告（或从缓存读取）
  - 不再消耗次数（同一次测算的补充内容）

---

## 📊 PHASE 6 · 报告展示页（前端）

### 6.1 报告页（`/result`）

- [ ] 顶部：缘分总分大数字动画（从 0 计数到最终分值，耗时 1.5s）+ 等级标签 badge
- [ ] 五行相合模块：双方五行用彩色标签展示（金=黄、木=绿、水=蓝、火=红、土=棕），一句话总结
- [ ] 缘分亮点：3-5 条，每条用卡片展示，带图标（✨）
- [ ] 相处建议：3 条，列表展示
- [ ] 专属姻缘签：独立卡片，居中大字展示，背景渐变，有「复制签文」按钮
- [ ] 付费墙：「隐患预警」和「最佳发展时机」模块用毛玻璃模糊效果遮盖，显示「完整版」badge + 引导文案 + 「获取完整版」按钮
- [ ] 点击「获取完整版」弹出 Modal，展示：小红书账号名 + 私信引导文案 + 价格说明
- [ ] 完整码用户直接展示全部内容，无付费墙
- [ ] 底部：分享区域（保存图片卡片 + 复制文字按钮）

### 6.2 分享图片卡片

- [ ] 使用 `html2canvas` 将卡片 DOM 转为 PNG
- [ ] 卡片设计（1080×1080px 比例）：
  - 背景：深色国风纹理（可使用 CSS 渐变模拟）
  - 顶部：产品名「八字缘分测算」+ 红色印章风格 Logo
  - 中部：缘分分数大字 + 等级标签
  - 下部：专属姻缘签文字（限 2 行）
  - 底部：「扫码测你的缘分」+ 二维码占位图（用 qrcode.js 生成当前页面链接）
- [ ] 点击「保存图片」触发下载，文件名为「我的缘分测算.png」
- [ ] 点击「复制文字」将分享文案复制到剪贴板，展示「已复制」toast

---

## 🛠 PHASE 7 · 运营后台（前端 + 后端）

### 7.1 后端：管理员接口

- [ ] 所有 `/api/admin/*` 接口需要 `Authorization: Bearer {ADMIN_PASSWORD}` 请求头校验
- [ ] `POST /api/admin/codes/generate`：接收 `{ count, type, expiresInDays, note }`，批量生成激活码，返回生成的码列表
- [ ] `GET /api/admin/codes/list`：支持 `?type=&used=&page=` 查询参数，返回激活码列表和统计数据
- [ ] `POST /api/admin/codes/revoke`：接收 `{ code }`，将该码 `used_count` 设为 `total_uses`（即作废）
- [ ] `GET /api/admin/stats`：返回总测算次数、今日测算次数、各类型码数量等统计

### 7.2 前端：运营后台（`/admin`）

- [ ] 访问 `/admin` 时弹出密码输入框，密码正确后存入 sessionStorage，错误则拒绝访问
- [ ] Tab 1「生成激活码」：
  - 表单：数量（10/50/100/自定义）、类型（体验/完整/无限）、有效天数、备注
  - 点击「生成」调用接口，将返回的码列表展示在下方，一键复制全部
- [ ] Tab 2「激活码列表」：
  - 表格展示：码 / 类型 / 已用/总次数 / 过期时间 / 备注 / 操作（作废）
  - 支持按类型、是否用完过滤
- [ ] Tab 3「数据统计」：展示总测算次数、今日新增、各类型码使用情况

---

## 🧪 PHASE 8 · 测试

### 8.1 后端单元测试

- [ ] 安装 Jest，配置测试脚本 `npm test`
- [ ] 测试 `baziEngine.js`：
  - [ ] 至少验证 10 个已知生日的八字计算结果（从权威网站对照）
  - [ ] 测试边界：立春前后的年柱切换、夜子时的时柱计算
  - [ ] 测试合婚评分：六合、六冲、三合等关系是否正确影响分数
- [ ] 测试 `codeService.js`：
  - [ ] 生成的码格式是否正确（`XXXX-XXXX`，无混淆字符）
  - [ ] 验证码：有效码 / 过期码 / 用完的码 / 不存在的码 各自返回正确结果
  - [ ] 并发消耗：同一个码同时被消耗 2 次，最终 used_count 是否正确递增（不超出 total_uses）

### 8.2 API 集成测试

- [ ] 使用 Postman 或 `.http` 文件编写以下测试用例：
  - [ ] 正常流程：验证码 → 生成报告（体验码）→ 确认次数减少
  - [ ] 完整码流程：验证码 → 生成报告 → 获取完整报告（含 warning/timing）
  - [ ] 次数耗尽：使用完的体验码再次调用 `/api/calculate` 返回 403
  - [ ] 无效激活码：返回正确错误信息
  - [ ] 管理员接口：无 token / 错误 token 返回 401；正确 token 正常使用

### 8.3 前端功能测试

- [ ] 激活码输入页：自动格式化（插入`-`）、大写转换、错误提示、成功跳转
- [ ] 生日输入：所有字段必填校验、时辰「不知道」可选
- [ ] 问卷：前进/后退、最后一题提交、跳题阻断
- [ ] Loading 页：动效正常、请求失败重试
- [ ] 报告页：分数动画、免费内容展示、付费墙遮盖效果、完整码无付费墙
- [ ] 分享功能：图片生成与下载、复制文字 toast

### 8.4 兼容性测试

- [ ] 在以下环境测试完整流程：
  - [ ] iOS Safari（iPhone 12 及以上）
  - [ ] Android Chrome
  - [ ] 微信内置浏览器（iOS + Android）
- [ ] 重点检查：Vant DatePicker 在微信浏览器中的兼容性、html2canvas 图片生成、复制到剪贴板 API

### 8.5 性能与安全测试

- [ ] 接口限流：配置 express-rate-limit，`/api/calculate` 每个 IP 每分钟最多 5 次请求
- [ ] 激活码暴力枚举防护：连续 5 次验证失败的 IP 封禁 10 分钟
- [ ] DeepSeek API 超时测试：模拟 API 超时，确认重试机制和前端错误处理正常
- [ ] 图片卡片生成压力测试：快速点击「保存图片」多次，确认不会重复触发

---

## 🚀 PHASE 9 · 部署与上线

### 9.1 前端部署（Vercel / Netlify / Static Host）

- [ ] 在 Vercel 创建项目，关联仓库根目录（含 `src/`），或将前端分离到 `frontend/` 时指定该目录
- [ ] 配置环境变量：`VITE_API_BASE_URL` 指向 Supabase Function 或后端 URL，`VITE_SUPABASE_URL`、`VITE_SUPABASE_ANON_KEY`（按需）
- [ ] 确保构建命令与输出目录（`npm run build` → `dist`）正确配置

```json
{
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
```

- [ ] 触发首次部署，验证页面可访问
- [ ] 配置自定义域名（可选）

### 9.2 后端部署（Railway）

- [ ] 在 Railway 创建项目，关联 GitHub 仓库 `backend/` 目录
- [ ] 配置所有环境变量：`SUPABASE_URL`、`SUPABASE_SERVICE_KEY`、`DEEPSEEK_API_KEY`、`ADMIN_PASSWORD`、`FRONTEND_URL`（用于 CORS 白名单）
- [ ] 确认 Railway 自动检测 `package.json` 的 `start` 脚本并启动服务
- [ ] 验证后端健康检查接口 `GET /health` 返回 200

### 9.3 上线前检查清单

- [ ] 所有环境变量已在生产环境配置，无 `.env` 文件泄露
- [ ] 后端 CORS 仅允许 Vercel 域名（非 `*`）
- [ ] Supabase RLS 已启用，非 service key 无法直接写入激活码表
- [ ] 管理员后台路由在生产环境可访问，密码足够复杂
- [ ] 预先生成 50 个体验码 + 20 个完整码备用
- [ ] DeepSeek API 余额充足（建议充值 ¥50 起步）
- [ ] 在真机（iOS + Android + 微信）完整走一遍测算流程
- [ ] 分享图片卡片下载正常，二维码指向正确的生产环境链接

### 9.4 上线后监控

- [ ] 在 Railway Dashboard 配置内存/CPU 告警
- [ ] 在 Supabase Dashboard 监控数据库连接数
- [ ] 配置简单的错误上报（可用 `console.error` 先记录到 Railway 日志，后续接入 Sentry）
- [ ] 上线后前 48 小时每日查看 Railway 日志，确认无异常报错

---

## 📋 附录：关键配置参考

### 激活码生成算法参考

```javascript
function generateCode() {
  const chars = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789'; // 排除 O/0/I/1
  const part = () => Array.from({ length: 4 }, () => chars[Math.floor(Math.random() * chars.length)]).join('');
  return `${part()}-${part()}`;
}
```

### DeepSeek API 调用参考

```javascript
const response = await fetch('https://api.deepseek.com/chat/completions', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${process.env.DEEPSEEK_API_KEY}`
  },
  body: JSON.stringify({
    model: 'deepseek-chat',
    messages: [
      { role: 'system', content: SYSTEM_PROMPT },
      { role: 'user', content: userPrompt }
    ],
    response_format: { type: 'json_object' },
    max_tokens: 1000,
    temperature: 0.8
  })
});
```

### 八字时柱天干推算表

```
日干  甲/己  乙/庚  丙/辛  丁/壬  戊/癸
子时   甲子   丙子   戊子   庚子   壬子
丑时   乙丑   丁丑   己丑   辛丑   癸丑
...（以此类推）
```

---
